node_modules/
dist/
.env
.env.local
*.local
.DS_Store

# Material estratégico interno — não publicar no repo público
docs/
definicoes.txt
scripts/ig/

# Config local da máquina / artefatos de build
.claude/settings.local.json
.mcp.json
*.tsbuildinfo
supabase/.temp/

# Telemetria local de custo de agentes IA (não publicar no repo público)
cost.txt

# Scripts de teste local da integração Wellhub (não versionar)
/validate.mjs
/simular.mjs
/webhook-e2e.mjs

# Logs de workflow baixados para depuração (contêm dados de produção)
logs_*/

# Exports de dados reais de aluno (nome/e-mail/telefone) — nunca no repo público
*.csv

# Pessoais
proximos-passos.txt
CLAUDE.md